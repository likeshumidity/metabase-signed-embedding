# Metabase Signed Embedding for Testing

## Setup

- Run `yarn` to install dependencies
- Copy `sample.env` to `.env` and update the variables for your signed embedding scenario

## Running

- Run `yarn dev` to run locally
